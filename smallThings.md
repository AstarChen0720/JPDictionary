這裡來記錄所有待解決的小任務

- 改讓進去網站後可以默認訪客模式而不是直接先跳登入牆
- 讓輸入框如果離開視角會懸浮在最上面你點會直接跳過去
- 讓跳到櫃台的魔法單純是focus到輸入框不用跳轉視窗
- 輸入框限制讓日文過,其他擋
- 讓每個快速選單標籤有叉叉,且可以設定自動定時刪除(預設30min)或保留資料
- 把沒登入預設改成只要重新整理資料就會不見
- 讓單字卡不要那麼大,並在上下加入按鈕方便快速切換(或按上下也可以)
- 單字卡加個演變,某東西如何從原型變過來的