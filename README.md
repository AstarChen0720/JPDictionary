目前就是簡單的一個查日文單字的字典

因為目前的市面上的都讓我覺的少了什麼所以想自己寫一個

結果目前是想樣用ai,因為沒有好的中文-日文翻譯線上api

目前比較特色的功能就是可以隨時貼上跟打字
然後歷史結果是會自動往下排的且會在左邊有一個類似書籤的東西方便回故歷史

上帝的建築工地總結（JPDictionary 專案）
上帝（開發者）： 你是最高意志，掌管一切，負責編寫「規則」與「要求」。

網頁（房子）：

HTML： 房子的硬體設施（牆壁、屋頂、窗戶）。

CSS： 房子的軟裝與材質（顏色、壁紙、甚至是特定材質的牆）。

JavaScript： 自動觸發的規則（例如有人進廚房就開燈）。

瀏覽器（工人派遣公司）： 負責在各種環境（地形）下，按照你的要求蓋出一模一樣的房子，並將複雜的硬體轉化為 DOM（建築圖） 方便管理。

React（主管）： 因為工人太笨，上帝請來主管。上帝只需對主管下簡單的命令，主管會負責處理繁瑣的細節。

Vite（工頭/翻譯官）： 主管（React）講的話工人聽不懂，需要工頭翻譯。且工頭很聰明，會實行「局部施工」，改窗戶就只改窗戶，不拆整棟房。

Node.js（基礎設施）： 工地現場的平地、自來水與電力系統，沒有它，任何工頭或工人都無法開工。

npm（物流中心/服務商）： 所有在 Node.js 基礎上運作的東西、家具、甚至是請主管（React）或工頭（Vite）的勞務服務，都從這裡叫貨。

原型屋（Vite 預設專案）： 工頭一來就先用他帶來的工具和 React 主管，在 localhost:5173 這片工地上搭好了一個樣品屋給你測試。

第一個房間是便當店:客人點餐後(輸入文字),員工會根據點的餐,將不同的菜放到對應的菜的位置(就像是1號套餐,你一定會有主菜,飯,菜1,菜2,湯.但是每個套餐的菜都不同,但是位置的格式是一樣的)

前期目標功能
1. 有搜尋框可以搜尋東西--有櫃檯可以點餐
2. 可以隨時跳轉到搜尋框(按鍵盤ctrl+enter之類的,或滑鼠指到搜尋框)--有一按鈕可以隨時傳送到櫃檯
3. 新的結果會在最上面,而舊的不會消失,而是被"擠"下去,排到下面,然後左側會有類似網站索引的超連結,按一下就會將視角移到那個單字的解果(每個單字都佔一整個螢幕,這樣比較簡潔)
4. 點餐後會根據我點的餐(輸入的文字)給我對應的便當(單字搜尋結果)

網站製作順序:像是打怪升級一樣從lv 1升級上去
網站等級LV:
1. 搜尋框輸入文字後下面會顯示單字卡(點餐後先給對應菜名的紙板)
2. 然後做新增單字後會把舊的擠下去跟索引標籤
3. 接上AI給出真正的字典結果
4. 接上google的tts(因為有免費額度)為例句和單字加上讀音
5. 線上資料保留與google快速登入
6. 上線讓人能瀏覽
7. 重構結構讓後面好維護和更新
8. 產品化:讓網站更加可用
9. 改良單字卡1:讓單字卡更加讓人一看就了解意思

LV3要分幾個小步驟:先決定要跟哪家中央廚房合作,並簽約,然後約定好我們怎麼下單後他要煮什麼菜品回來給我們(餐品製作SOP合約),最後將他送回的菜裝進我們的便當後出餐
  1. 決定api(決定要跟誰合作)
  2. 制定規則(約定好合約)並測試
  3. 自動傳送和分類他回覆的指令(新的點餐SOP)
  4. 顯示出來(出餐)

LV4要分的步驟:現在每個便當都有很多字條顯示便當名或菜名,但是有些名子太刁鑽不會念,客人要問這怎麼念,餐廳內人員會念但是不標準(也不是很會XD)所以現在。要將顧客的問題轉寄去專業播音公司請他們幫我們唸(他們公司內有專業人員可以念的又標準又自然),然後播音公司會將結果放CD寄回,再將這CD放入撥放器撥給客人聽(這次不用駐點人員,因為這次需求的服務比較簡單只要寄信就可以了)
  1. 先決定要跟哪家播音公司合作.並拿到會員卡(apikey)
  2. 設定好播音SOP
     1. 將客人要問的字跟委託書(例如讀音要怎樣,這是日文還英文)一起寄給他們公司
     2. 他會寄回一cd,將其放入撥放器播放出聲音
  3. 將可以讓客人詢問的字條旁邊放一個小按鈕,按下他相當於問這怎麼念,而這時就會處發播音SOP
  4. 將拿到的CD先放好不要播完就丟掉(緩存),這樣客人要重複問時就不用我們再寄信去問

LV5要分成兩部分:資料同步,用戶認證(註冊/登入)和資料隔離
資料同步:就像是將資料複製一份到倉庫,即是店拆了(unload)下次再蓋時,只要再去倉庫拿就可以有之前的資料
  1. 找要合作的倉儲公司,並且約定好倉庫要怎樣存東西(例如跟他說我到時候會一次寄蘋果,香蕉,芭樂,然後你幫我一種放一堆,而不是全都直接丟倉庫),然後拿到會員卡,請駐點人員過來店裡
   2. 寫增刪改查SOP,因為所有要跟倉庫的互動都能簡化成"增刪改查"四種動作,但都要透過倉儲駐點人員,且有些還有包包裹,拆包裹等步驟,所以定成SOP比較方便
    2.1向倉庫增加東西的SOP
    2.2將倉庫某東西刪除的SOP
    2.3將倉庫某東西修改的SOP
    2.4向倉庫查取東西的SOP
  3. 改點餐SOP,讓每次做好便當要出餐時就將這出餐紀錄,讓倉儲駐點人員寄去倉儲公司存進倉庫存起來(執行向倉庫增加東西SOP)
  4. 向倉庫同步資料,讓開店時(或換不同客人時(以後在改)),向倉庫查取客人資料,並將目前的資料撕掉,將查到的抄上去
  5. 刪除倉庫資料,讓歷史清單的右上有一個叉,按下就會刪除這單字卡,同時刪除倉庫內的同一筆資料(執行將倉庫某東西刪除SOP)
  6. 修改倉庫資料,讓便當清單的每一單字卡的右下角有一個"修改"的按鈕,按下後就可以修改文字,改完後倉庫內的資料也會同步修改(執行將倉庫某東西修改SOP)
用戶認證(註冊/登入)和資料隔離:顧客如果註冊會員然後給了email密碼了,會再倉庫保險庫新增一個對應表和用戶id,並傳回通行證(上面會寫會員資料,這樣每次要存取東西就不用再確認一次)(有些要驗證才會發回來),會請一保全持續監視有沒有新的通行證,如果有就去更新筆記本
登入一樣,如果有對應就傳回通行證
登出就是刪除通行證,這樣你就要重新傳email密碼給他驗證了
資料隔離就是去supabase設定只有對應通行證的人才可以增刪改查對應會員的東西
  1. 準備工作:拿筆記本紀錄通行證(useState),把addToSpabase上傳的東西加上user_id欄位,~~讓fetchFromSupabase函式可以傳除user_id做為目標~~(本來需要但是因為supabase的RLS規則就會過濾所以就不用特別寫了)
  2. 修改開店任務,讓開店檢查有沒有現有通行證,有就抓資料
  3. 新增監視通行證任務,請一個人持續檢查通行證狀態(登入狀態),如果有變化就更新通行證筆記內容到最新
  4. 新增同步資料任務,登入則從倉庫抓資料,登出就清空資料
  5. 將supabase給的註冊登入登出三個函式寫好方便調用
  6. 寫ui,如果有通行證就直接顯示網站,沒有就覆蓋一個登入/註冊的畫面,並且將supabase給的註冊登入登出三個函式綁定到ui上
  7. 去supabase設定RLS

LV6上線讓人能瀏覽:
用vercel部署了網站,(記得要讓他的根目錄是main)
網址[jp-dctionary.vercel.app](https://jp-dctionary.vercel.app/)


LV7重構讓後面好加東西和更新:將工頭規則書分開寫
重構就是將現在全都寫在一起的功能分開寫,讓app.jsx只管總調度,讓邏輯清楚好懂
  1. 將每個功能分開 

LV8產品化:讓網站真正可用,整理更改使用邏輯讓變成更加可用
  1. 讓不用強制登入,如沒有登入存在localstorage,並移除登入牆,localStorage就像是本地儲物箱,但是也是需要將便當包裝好才能放入才不會壓壞東西 
     1. 寫本地增刪改查的SOP,並整合supabase的那四個,變成四個自動用會員有沒有登入來判斷存本地還是存supabase的函式,並且將呼叫都改成這通用的函式
     2. 改同步資料任務讓剛開店(他開店會先執行一次)或登入狀態有變,就檢查有沒有登入,有登入就先上傳後刪除本地最後下載,沒有登入就將本地資料抄筆記本上
     3. 在主頁面左上角加入一個登入/註冊按鈕,如果沒有登入登出就是訪客模式,按下跳出登入/註冊畫面(本來的登入牆)

LV9改良單字卡1:讓單字卡更加讓人一看就了解意思
  先改良通用佈局,再從日文五大詞類分別改良,最後改良寫進句子
  改良通用佈局:反正就是全部單字有關的都壓縮在一起,下面盡量只放例句
     1. 讀音跟單字做在一起,點選單字(整個div)可以念
     2. 單字右邊有假名,標有線條高低音(像10ten-jp那樣)
        1. 改gemini指令,讓他回我每個字的高低音
        2. 用邊框幫每個高音上邊框,低音下邊框,高低音交接處則是右側邊框
        3. 單字顯示不再用假名而是改用flexbox方便排版
     3. 單字上加上完整片假名(用ruby讓可以跟漢字對齊),後面有數字音標,最右有詞性(像是い形容詞)<--現在在這
     4. 同音字用左右選單(單字的顯示和讀音會變,且顏色不同方便辨認)
     5. 同字不同意思就左邊放上下選單,點就可以跳轉到不同頁面
    同音字mazii是全讀音放上面,再分別將不同意思例句顯示出來
    moji則是一開始就讓你選
    
    因為同音字通不同音通常都不同意思,所以同音字用左右選單
  1. 加個演變,某東西如何從原型變過來的
  2. 例句要有戲劇性,且除了重點句外都用中文讓好理解意思
  ex:(在魔王城外帳篷內,明天就要進攻)A:各位我們一定要<日文>成功<日文>!!
  
  1. 
請駐點人員進駐： 在 App.jsx 裡引入他們的工具包（安裝套件）。

簽署入庫契約： 設定好你的許可證（API Key），讓駐點人員知道他服務的是哪家店。

改寫點餐 SOP： 以前 takeOrder 是把內容抄在筆記本（useState），現在除了抄筆記本，還要多一個動作：「交給駐點人員寄回總部」。

開店領貨流程： 在便當店剛開門時（useEffect），主動請駐點人員問公司：「有沒有我們店之前的存貨？」有的話就通通搬回來放到筆記本裡。